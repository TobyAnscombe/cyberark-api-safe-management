---
name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
jobs:
  secret-scan:
    uses: TobyAnscombe/github-actions/.github/workflows/gitleaks.yml@4ddc41ac0ffb2dc6a1fa4a0448525b3d9254c989
    permissions:
      security-events: write

  ansible-lint:
    uses: TobyAnscombe/github-actions/.github/workflows/ansible-ci.yml@4ddc41ac0ffb2dc6a1fa4a0448525b3d9254c989
    with:
      ansible_lint_version: "26.1.0"
      requirements_file: "requirements.yml"

  claude-file-check:
    uses: TobyAnscombe/github-actions/.github/workflows/claude-file-check.yml@4ddc41ac0ffb2dc6a1fa4a0448525b3d9254c989

  protect-main:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: [secret-scan, ansible-lint, claude-file-check]
    uses: TobyAnscombe/github-actions/.github/workflows/branch-protection.yml@4ddc41ac0ffb2dc6a1fa4a0448525b3d9254c989
    secrets:
      token: ${{ secrets.BRANCH_PROTECTION_TOKEN }}
    with:
      required_checks: '["secret-scan / Scan for secrets", "ansible-lint / Lint Ansible", "claude-file-check / claude-file-check"]'
